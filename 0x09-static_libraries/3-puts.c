#include "main.h"
void _puts(char *str)
#include <stdio.h>

	/**
	 * main - Entry poiint
	 *
	 * Return: Always 0 (Success)
	 */

	int main(void)
{
	puts("\"Programming is like building a mulitlingual puzzle");
	return (0);
}
